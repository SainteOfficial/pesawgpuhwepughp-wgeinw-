# MythHub Git-Sync`nwhile($true) {`n  git add .`n  git status --porcelain | Out-Null`n  if ($LASTEXITCODE -eq 0) {`n    git commit -m "Automatische Aktualisierung von MythHub"`n    git push`n    Write-Host "Änderungen wurden hochgeladen!" -ForegroundColor Green`n  } else {`n    Write-Host "Keine Änderungen gefunden." -ForegroundColor Yellow`n  }`n  Write-Host "Warte 60 Sekunden..." -ForegroundColor Gray`n  Start-Sleep -Seconds 60`n}
